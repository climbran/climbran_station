<li>
  <%= link_to article.title, article %>

  <% if logged_in? && logged_admin? %>
      | <%= link_to "删除", article, method: :delete,
                    data: { confirm: "确认删除？" } %>
  <%end%>
</li>