
  <div class='span3 sidebar'>
    <h2>最新文章</h2>
    <ul class="nav nav-tabs nav-stacked">
      <% Article.order(created_at: :desc).take(8).each do |article| %>
          <li><%= link_to article.title , article %></li>
      <% end %>
    </ul>
    <h2><%= link_to "所有文章", articles_path %></h2>
  </div>
