<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "CLIMBRAN", root_path, id:"logo" %>
    <nav>
      <ul class="nav navbar-nav pill-right">
        <li><%= link_to "首页", root_path %></li>

        <li class="dropdown">
          <a href="" class="dropdown-toggle" data-toggle="dropdown">
            文章 <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <% Genre.all.each do |g| %>
            <li><%= link_to g.name, articles_path(:genre_id => g.id)%></li>
            <% end %>
            <li class="divider"></li>
            <li>
              <%= link_to "所有文章", articles_path %>
            </li>
          </ul>
        </li>
        <% if logged_in? %>
            <li class="dropdown">
              <a href="" class="dropdown-toggle" data-toggle="dropdown">
                管理 <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "新文章", new_article_path %></li>
                <li><%= link_to "文章管理", '' %></li>
                <li><%= link_to "信息/管理", current_user %></li>
                <!--li><!%= link_to "设置", edit_user_path(current_user) %></li-->
                <li class="divider"></li>
                <li>
                  <%= link_to "注销", logout_path, method: "delete" %>
                </li>
              </ul>
            </li>
        <% else %>
            <li><%=link_to "登陆",login_path %></li>
        <% end %>
        </ul>
    </nav>
  </div>
</header>